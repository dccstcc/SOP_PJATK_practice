#include <stdio.h>

int main(void) {
    float halfValue = 0.6 - 0.1;
    float piValue = 3.14159265 - 3 + 1;
    printf("the value of half is: %f\n", halfValue);
        printf("the value of pi is: %f\n", piValue);
    return 0;
}
