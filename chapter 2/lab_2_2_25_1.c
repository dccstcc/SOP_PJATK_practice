#include <stdio.h>

int main(void) {
    int xValue = 5;
    int yValue = 9;
    int result =((xValue + yValue) *2);
    int bigResult = (xValue + yValue *6);



    printf("result: %d\n", result);
        printf("big result: %d\n", bigResult);
    return 0;
}
