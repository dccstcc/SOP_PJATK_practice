#include <stdio.h>

int main(void) {

    int daysInWeek;
    float pi;

    printf("How many days in the week: ");
    scanf("%d", &daysInWeek);
    printf("The value of PI to two points: ");
    scanf("%f", &pi);
    printf("There are %d weeks in the week.\n", daysInWeek);
    printf("PI value is %7.6f.", pi);

return 0;
}
